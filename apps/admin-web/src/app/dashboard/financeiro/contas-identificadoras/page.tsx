"use client";

import CadastroDeContas from "@/components/financeiro/cadastro/CadastroDeContas";

/**
 * 🏷️ TELA: CADASTRO DE CONTAS IDENTIFICADORAS (PJODC v10)
 * Por que o dinheiro se moveu — despesa, receita e outras.
 */
export default function ContasIdentificadorasPage() {
  return <CadastroDeContas variante="identificadora" />;
}
